VITE_API_BASE_URL=http://localhost:4000/api/admin
VITE_COMMON_IMAGE_URL=http://localhost:4000
# sehat-potli-front's own URL — only used to build the "Login as Customer"
# link (opens ${VITE_STORE_FRONT_URL}/api/impersonate?token=...) in a new tab.
VITE_STORE_FRONT_URL=http://localhost:3000
